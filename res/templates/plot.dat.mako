# ${name} scalability - bit array (index 0)
# ${xlabel}   ${ylabel}
% for x, y in bit_data:
  ${x}       ${y}
% endfor


# ${name} scalability - bool array (index 1)
# ${xlabel}   ${ylabel}
% for x, y in bool_data:
  ${x}       ${y}
% endfor
